export const deepFreeze = (value)=>Object.isFrozen(value) ? value : Array.isArray(value) ? Object.freeze(value.map(deepFreeze)) : deepFreezeDictionary(value);
const deepFreezeDictionary = (value)=>{
    for(const k in value){
        deepFreeze(value[k]);
    }
    return value;
};
