import type { YrelError, YrelSchema, InferYrel } from './types';
interface ValidateYrelIssue {
    key: string;
    errors: YrelError[];
}
declare const validateYrel: <Schema extends YrelSchema<any>>(schema: Schema, data: unknown, options?: {
    rootKey?: string;
}) => {
    isValid: true;
    issues: never[];
    data: InferYrel<Schema>;
} | {
    isValid: false;
    issues: ValidateYrelIssue[];
    data: undefined;
};
export type { ValidateYrelIssue };
export { validateYrel };
