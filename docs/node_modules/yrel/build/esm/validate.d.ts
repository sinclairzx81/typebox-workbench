import type { DataError, DataSchema, InferDataSchemaType } from './types';
interface Issue {
    key: string;
    errors: DataError[];
}
declare const validate: (schema: DataSchema, data: unknown, options?: {
    rootKey?: string;
}) => {
    isValid: boolean;
    issues: Issue[];
    data: any;
};
export { validate };
